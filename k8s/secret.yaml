apiVersion: v1
kind: Secret
metadata:
  name: getsocial-secrets
type: Opaque
stringData:
  SECRET_KEY: "change-me-in-production"
  # Add DB_PASSWORD etc here if not using connection string in ConfigMap
